[package]
name = "warp_graphql"
version = "0.1.0"
edition = "2021"
authors.workspace = true
license.workspace = true
publish = false
description = "Minimal warp_graphql stand-ins for wormhole-slim embed builds"

[dependencies]
cloud_objects = { path = "../inline/cloud_objects" }
chrono = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
warp_managed_secrets = { path = "../inline/managed_secrets" }
